module CrossRef :> sig

  val register : string -> string -> unit
  val get : string -> option string

end = struct

  val register value label =
    register-cross-reference label value

  val get label = get-cross-reference label

end
