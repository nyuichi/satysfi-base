use package open Math
use package open Base
use open TestUtil of `./../util`

let open Typeset in
let open Derive in
satysfi-it ${
  \proven!(
    let open DeriveDSL in
    derive ${A \vdash A}
  )
}
