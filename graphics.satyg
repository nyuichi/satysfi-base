@require: pervasives

module Graphics : sig
  val shift : point -> graphics -> graphics
  val bbox : graphics -> (length * length) * (length * length)
  val stroke : length -> color -> path -> graphics
  % dashed-stroke
  % fill
  % stroke
  % draw-text
end = struct
  let shift = shift-graphics
  let bbox = get-graphics-bbox
  let stroke = stroke
end